TypeScript React AntDesign Table Scroll Demo
=================================

只要设置了 `scroll` 就表示要滚动，此时table本身的大小就像一个窗口，内部通过 `x/y` 来设置下面内容的真实宽和高。

其中scroll.x/y除了支持数字（像素）外，也支持`vh/vw`

```
npm install
npm run demo
```

It will open page on browser automatically.
